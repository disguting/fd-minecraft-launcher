<UserControl x:Class="launcher_m.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             Foreground="White"
             Opacity="0">
    <UserControl.RenderTransform>
        <TranslateTransform Y="20"/>
    </UserControl.RenderTransform>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.25">
                        <DoubleAnimation.EasingFunction>
                            <QuarticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                    <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" To="0" Duration="0:0:0.25">
                        <DoubleAnimation.EasingFunction>
                            <QuarticEase EasingMode="EaseOut"/>
                        </DoubleAnimation.EasingFunction>
                    </DoubleAnimation>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <UserControl.Resources>
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <ContentPresenter />
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Margin="40">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="280"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{DynamicResource Loc_MyInstances}" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

            <ListView x:Name="ListInstancesHome" 
                      Grid.Row="1" 
                      Background="Transparent" 
                      BorderThickness="0" 
                      HorizontalContentAlignment="Stretch"
                      ClipToBounds="False"
                      VirtualizingPanel.IsVirtualizing="True" 
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      ScrollViewer.CanContentScroll="True">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,0,30,12" Background="Transparent" RenderTransformOrigin="0, 0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Grid.RenderTransform>

                            <Grid.Style>
                                <Style TargetType="Grid">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Trigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1.015" Duration="0:0:0.1"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1.02" Duration="0:0:0.1"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.EnterActions>
                                            <Trigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" To="1" Duration="0:0:0.15"/>
                                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" To="1" Duration="0:0:0.15"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </Trigger.ExitActions>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Grid.Style>

                            <Border x:Name="SelectionHighlight" 
                                    Background="#154CAF50" 
                                    BorderBrush="#FF4CAF50" 
                                    BorderThickness="1.5" 
                                    CornerRadius="4" 
                                    Opacity="0"/>

                            <ui:CardAction Padding="15" Click="Card_Click" Background="Transparent" BorderThickness="1" BorderBrush="#1AFFFFFF" IsChevronVisible="False">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <Border x:Name="IconBorder" Width="50" Height="50" CornerRadius="8" Background="#15FFFFFF" Margin="0,0,15,0">
                                        <ui:SymbolIcon x:Name="IconMain" Symbol="{Binding IconSymbol, FallbackValue=Box24}" FontSize="28" Foreground="#FF9E9E9E"/>
                                    </Border>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <TextBlock x:Name="TxtTitle" Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,4" Foreground="White"/>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding GameVersion}" FontWeight="Medium" Foreground="#FFDDDDDD" Margin="0,0,5,0"/>
                                            <TextBlock Text="{Binding LoaderType, StringFormat='• {0}'}" Foreground="#FFAAAAAA" Margin="0,0,15,0"/>
                                            <ui:SymbolIcon x:Name="CheckIcon" Symbol="CheckmarkCircle24" FontSize="14" Foreground="#4CAF50" Visibility="Collapsed" Margin="0,0,5,0"/>
                                            <TextBlock x:Name="TxtStatus" Text="{DynamicResource Loc_ReadyToPlay}" FontSize="12" Foreground="#FFAAAAAA" VerticalAlignment="Center"/>
                                        </StackPanel>
                                    </StackPanel>

                                    <ui:Button Grid.Column="2" 
                                       Appearance="Transparent" 
                                       Background="Transparent"
                                       BorderThickness="0"
                                       Padding="8,8" 
                                       Margin="10,0,0,0" 
                                       VerticalAlignment="Center" 
                                       Click="BtnMoreOptions_Click">
                                        <ui:SymbolIcon Symbol="MoreVertical24" FontSize="20" Foreground="#FFAAAAAA"/>

                                        <ui:Button.ContextMenu>
                                            <ContextMenu>
                                                <MenuItem Header="{DynamicResource Loc_EditInstance}" Click="MenuEditInstance_Click">
                                                    <MenuItem.Icon>
                                                        <ui:SymbolIcon Symbol="Edit24"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>

                                                <Separator Background="#33FFFFFF" Margin="0,5"/>

                                                <MenuItem Header="{DynamicResource Loc_MenuMoveUp}" Click="MenuMoveUp_Click">
                                                    <MenuItem.Icon>
                                                        <ui:SymbolIcon Symbol="ArrowUp24"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>

                                                <MenuItem Header="{DynamicResource Loc_MenuMoveDown}" Click="MenuMoveDown_Click">
                                                    <MenuItem.Icon>
                                                        <ui:SymbolIcon Symbol="ArrowDown24"/>
                                                    </MenuItem.Icon>
                                                </MenuItem>
                                            </ContextMenu>
                                        </ui:Button.ContextMenu>
                                    </ui:Button>

                                </Grid>
                            </ui:CardAction>
                        </Grid>

                        <DataTemplate.Triggers>
                            <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListViewItem}, Path=IsSelected}" Value="True">
                                <DataTrigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionHighlight" Storyboard.TargetProperty="Opacity" To="1" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.EnterActions>
                                <DataTrigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="SelectionHighlight" Storyboard.TargetProperty="Opacity" To="0" Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </DataTrigger.ExitActions>

                                <Setter TargetName="IconMain" Property="Foreground" Value="#FF4CAF50"/>
                                <Setter TargetName="IconBorder" Property="Background" Value="#254CAF50"/>
                                <Setter TargetName="TxtTitle" Property="Foreground" Value="#FF4CAF50"/>
                                <Setter TargetName="CheckIcon" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="TxtStatus" Property="Text" Value="{DynamicResource Loc_Selected}"/>
                                <Setter TargetName="TxtStatus" Property="Foreground" Value="#FF4CAF50"/>
                            </DataTrigger>
                        </DataTemplate.Triggers>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <StackPanel Grid.Column="2">
            <TextBlock Text="{DynamicResource Loc_SupportTitle}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <ui:CardAction Margin="0,0,0,15" Padding="15" Background="#10FFD700" BorderBrush="#25FFD700" BorderThickness="1" Click="BtnSupportArmy_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:SymbolIcon Symbol="Heart24" FontSize="26" Foreground="#FFFFD700" Margin="0,0,15,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource Loc_SupportArmy}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,2" Foreground="White"/>
                        <TextBlock Text="{DynamicResource Loc_SupportArmyDesc}" FontSize="12" Foreground="#FFAAAAAA"/>
                    </StackPanel>
                </Grid>
            </ui:CardAction>

            <ui:CardAction Margin="0,0,0,15" Padding="15" Background="#104CAF50" BorderBrush="#254CAF50" BorderThickness="1" Click="BtnSupportAuthor_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:SymbolIcon Symbol="Gift24" FontSize="26" Foreground="#FF4CAF50" Margin="0,0,15,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource Loc_SupportAuthor}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,2" Foreground="White"/>
                        <TextBlock Text="{DynamicResource Loc_SupportAuthorDesc}" FontSize="12" Foreground="#FFAAAAAA"/>
                    </StackPanel>
                </Grid>
            </ui:CardAction>

            <ui:CardAction Padding="15" Background="#10F44336" BorderBrush="#25F44336" BorderThickness="1" Click="BtnReportBug_Click">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:SymbolIcon Symbol="Warning24" FontSize="26" Foreground="#FFF44336" Margin="0,0,15,0"/>
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{DynamicResource Loc_ReportBug}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,2" Foreground="White"/>
                        <TextBlock Text="{DynamicResource Loc_ReportBugDesc}" FontSize="12" Foreground="#FFAAAAAA"/>
                    </StackPanel>
                </Grid>
            </ui:CardAction>

        </StackPanel>
    </Grid>
</UserControl>